{
  "permissions": {
    "allow": [
      "Bash(gcloud builds describe *)",
      "Bash(gcloud builds list *)",
      "Bash(gcloud builds log *)",
      "Bash(python3 -m json.tool)",
      "Bash(make validate-prod)",
      "Bash(source /home/enot/workspace/shop_assistant/.venv/bin/activate*)",
      "Bash(git -C * diff *)",
      "Bash(source .venv/bin/activate*)",
      "Bash(node --check *)",
      "Bash(gcloud builds triggers list *)",
      "Bash(git -C * status*)",
      "Bash(git -C * log *)",
      "Bash(gcloud logging read *)",
      "Bash(gcloud run services describe *)",
      "Bash(gcloud config get-value *)",
      "Bash(disown*)",
      "Bash(cd *)",
      "Bash(source *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(git commit *)",
            "prompt": "A git commit was just made. Review the changes by running `git diff HEAD~1 HEAD`. Focus on: CSS/JS anti-patterns (e.g. display:flex/grid on elements that also use the HTML `hidden` attribute, which causes the hidden attribute to have no effect); security issues in FastAPI auth or cookie handling; correctness bugs in Python or JavaScript; missing test coverage for new code paths. Report only real findings — skip style nitpicks and issues already covered by the pre-commit ruff/pytest hooks.",
            "timeout": 120,
            "statusMessage": "Code reviewer checking commit..."
          }
        ]
      }
    ]
  }
}
